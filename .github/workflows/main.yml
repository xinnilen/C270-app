name: CI/CD Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      # Step 1: Checkout the code
      - name: Checkout code
        uses: actions/checkout@v2
        
      # Step 2: List repository contents (for debugging purposes)
      - name: List repository contents
        run: ls -R  # This will list all files and directories

      # Step 3: Set up Node.js environment
      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '14'

      # Step 4: Change directory to frontend (adjust if needed based on the debug output)
      - name: Change directory to frontend
        run: cd frontend  # Replace 'frontend' with the correct directory name based on ls output

      # Step 5: Install dependencies
      - name: Install dependencies
        run: npm install

      # Step 6: Run tests
      - name: Run tests
        run: npm test

  deploy:
    runs-on: ubuntu-latest
    needs: build

    steps:
      # Step 1: Checkout the code
      - name: Checkout code
        uses: actions/checkout@v2

      # Step 2: Set up Node.js environment
      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '14'

      # Step 3: Change directory to frontend (if necessary)
      - name: Change directory to frontend
        run: cd frontend  # Adjust as needed based on ls output

      # Step 4: Install dependencies
      - name: Install dependencies
        run: npm install

      # Step 5: Deploy (example, customize according to your needs)
      - name: Deploy to Production
        run: |
          ssh user@your-server "cd /path/to/your/project && git pull origin main && npm install && pm2 restart your-app"
        env:
          SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
          HOST: ${{ secrets.HOST }}
          USER: ${{ secrets.USER }}
